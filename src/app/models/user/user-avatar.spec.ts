import { UserAvatarModel } from './user-avatar-model';

describe('UserAvatar', () => {
  // it('should create an instance', () => {
  //   // @ts-ignore
  //   expect(new UserAvatarModel()).toBeTruthy();
  // });
});
